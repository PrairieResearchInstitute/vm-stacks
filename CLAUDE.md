# Illinois State Water Survey VM Config
This repo holds a set of docker-compose stacks for provisioning the ISWS 
virtual machine along with a script to bring all of the stacks up when 
the VM is booted.

It uses Mozilla's Secrets OPerationS to maintain secrets in this git repo which
will be a public repo.
